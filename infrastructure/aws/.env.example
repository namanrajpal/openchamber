# AWS Configuration
AWS_REGION=us-east-1
AWS_ACCOUNT=123456789012

# ============================================
# Domain & Certificate (REQUIRED - choose ONE mode)
# ============================================

DOMAIN_NAME=openchamber.yourcompany.com

# MODE 1: Auto Certificate & DNS (Recommended)
# CDK creates ACM certificate with DNS validation and Route53 A record
# Find your Hosted Zone ID: AWS Console → Route53 → Hosted Zones
#
HOSTED_ZONE_ID=Z0123456789ABCDEFGHIJ
HOSTED_ZONE_NAME=openchamber.yourcompany.com
#
# Example (uncomment and replace with your values):
# HOSTED_ZONE_ID=Z0123456789ABCDEFGHIJ
# HOSTED_ZONE_NAME=example.com

# MODE 2: Manual Certificate (Legacy)
# Provide pre-created ACM certificate ARN - you must manually create DNS CNAME
# If CERTIFICATE_ARN is set, it takes precedence over HOSTED_ZONE_ID
#
# CERTIFICATE_ARN=arn:aws:acm:us-east-1:123456789012:certificate/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# ============================================
# Authentication - Cognito
# ============================================

# Enable Cognito authentication (default: true)
COGNITO_ENABLED=true

# Option A: Create NEW User Pool (leave these blank)
# CDK will create a new User Pool, App Client, and Domain
# This is the default behavior if you don't provide COGNITO_USER_POOL_ID

# Option B: Use EXISTING User Pool (provide both values)
# CDK imports your pool and creates a new App Client
# Find your User Pool ID: AWS Console → Cognito → User Pools → Select Pool
# Find your Domain prefix: AWS Console → Cognito → User Pools → App Integration → Domain
#
# COGNITO_USER_POOL_ID=us-west-2_hZpW7gd5N
# COGNITO_DOMAIN_PREFIX=transformers-auth
#
# Example for existing pool (uncomment and replace with your values):
# COGNITO_USER_POOL_ID=us-west-2_hZpW7gd5N
# COGNITO_DOMAIN_PREFIX=transformers-auth

# Git Configuration (Optional)
GIT_USER_NAME=OpenChamber Bot
GIT_USER_EMAIL=openchamber@yourcompany.com

# Fargate Configuration (Optional)
FARGATE_CPU=4096
FARGATE_MEMORY=8192

# Stack Configuration (Optional)
STACK_NAME=OpenchamberStack
